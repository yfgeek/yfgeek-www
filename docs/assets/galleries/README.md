# Photo galleries

- Put travel photos in `travel/`.
- Put food photos in `food/`.
- Run `node outputs/build-galleries.mjs` from the project root after changing either folder.

The website also supports choosing a local photo directory directly from each gallery window.
